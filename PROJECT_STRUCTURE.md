# 末日房東模擬器 - 專案結構說明

## 📊 專案概述

這是一個基於 ES6 模組化架構的網頁遊戲，採用配置驅動和事件通信的設計模式。玩家扮演末日世界中的房東，管理資源、租客和各種生存挑戰。

**技術架構**：ES6 模組化 + 配置驅動 + 事件通信
**部署平台**：GitHub Pages（原生 ES6 支援，無需建置工具）
**開發狀態**：主要功能完成，僅剩少量模組待重構

## 🏗️ 檔案結構概覽

```
apocalypse-landlord-simulator/
├── README.md                          # 專案說明文件
├── PROJECT_STRUCTURE.md               # 本檔案 - 專案結構說明
├── .gitignore                         # Git版本控制設定
│
├── src/                               # 原始碼目錄
│   ├── index.html                     # 正式版本入口
│   ├── dev-test.html                  # 開發測試環境
│   │
│   ├── js/                           # JavaScript模組目錄
│   │   ├── main.js                    # 應用程式進入點
│   │   ├── Type.js                    # 型別定義系統
│   │   │
│   │   ├── core/                     # 核心系統模組（零依賴）
│   │   │   ├── DataManager.js        # 統一資料管理核心
│   │   │   ├── GameState.js          # 中央狀態管理系統
│   │   │   └── EventBus.js           # 事件通信系統
│   │   │
│   │   ├── systems/                  # 業務系統模組
│   │   │   ├── BaseManager.js        # 基礎管理器抽象類
│   │   │   ├── ResourceManager.js    # 資源流轉控制
│   │   │   ├── TradeManager.js       # 統一交易系統
│   │   │   ├── TenantManager.js      # 租客生命週期管理
│   │   │   ├── SkillManager.js       # 技能執行管理
│   │   │   ├── RentManager.js        # 租金計算與收取
│   │   │   ├── DayManager.js         # 每日事務處理
│   │   │   ├── UniversalTrader.js    # 通用交易處理器
│   │   │   └── EventManager.js       # 事件觸發處理（待重構）
│   │   │
│   │   ├── ui/                       # 使用者介面模組
│   │   │   ├── UICore.js             # 核心UI功能
│   │   │   ├── UIDisplay.js          # 畫面顯示邏輯
│   │   │   └── UIModal.js            # 彈窗系統管理
│   │   │
│   │   └── utils/                    # 工具函數模組
│   │       ├── constants.js          # 系統級常數定義
│   │       ├── validators.js         # 輕量驗證系統
│   │       └── helpers.js            # 配置驅動輔助函數
│   │
│   ├── data/                         # 遊戲配置目錄
│   │   ├── rules.json                # 遊戲規則配置（集中配置）
│   │   ├── tenants.json              # 租客類型配置
│   │   ├── skills.json               # 技能系統配置（15個技能）
│   │   └── events.json               # 事件系統配置
│   │
│   └── css/                          # 樣式檔案目錄
│       └── main.css                  # 主要樣式檔案
│
├── docs/                             # 專案文件目錄
│   ├── architecture.md              # 架構設計文件
│   ├── BaseManager-Guide.md          # BaseManager 開發指南
│   └── flow-diagrams.md              # 系統流程圖集
│
└── tests/                           # 測試檔案目錄（未來實作）
    └── unit/                        # 單元測試
```

## 📈 模組功能說明

### 🏗️ 核心系統模組
核心模組提供基礎設施，無外部依賴，是整個系統的基石。

- **DataManager.js** - 統一資料管理核心
  - 並行載入四個配置檔案（rules.json、tenants.json、skills.json、events.json）
  - 配置載入失敗時明確錯誤提示，確保系統穩定性
  - 提供統一存取介面：`getGameRules()`, `getTenantTypes()` 等

- **GameState.js** - 中央狀態管理系統
  - 深度複製保證狀態不變性
  - 狀態變更歷史追蹤
  - 路徑式狀態存取：`getStateValue('resources.cash')`
  - 資源管理輔助方法：`modifyResource()`, `hasEnoughResource()`

- **EventBus.js** - 事件通信系統
  - 標準發布/訂閱模式，支援模組間解耦通信
  - 一次性監聽和事件過濾機制
  - 完整事件統計和除錯功能

### 🛠️ 工具支援系統
提供通用功能和輔助工具，被其他模組廣泛使用。

- **constants.js** - 系統級常數管理（純技術常數）
- **validators.js** - 輕量驗證系統（可選啟用）
- **helpers.js** - 配置驅動輔助函數
- **Type.js** - 型別定義系統（JSDoc + TypeScript 檢查）
- **main.js** - 應用程式進入點（系統整合協調）

### 📊 配置驅動體系
所有遊戲參數都來自 JSON 檔案，實現配置與程式碼分離。

- **rules.json** - 集中遊戲規則配置（包含初始資源、建築成本等）
- **tenants.json** - 5種租客類型配置（包含屬性、技能等）
- **skills.json** - 15個技能完整配置（醫生3個、工人3個、農夫3個、軍人3個、長者3個）
- **events.json** - 四類事件系統配置（隨機、衝突、特殊、腳本事件）

### 💼 業務系統模組
實現核心遊戲邏輯，每個模組負責特定業務領域。

- **BaseManager.js** - 基礎管理器抽象類
  - 提供統一的事件通信、日誌記錄、狀態管理功能
  - 智慧事件前綴解析，支援系統級、業務領域、模組級分層命名
  - 所有業務管理器的基礎架構

- **ResourceManager.js** - 資源流轉控制核心
  - 統一資源修改介面、閾值監控、轉移驗證
  - 完整的資源狀態評估和警告系統

- **TradeManager.js** - 統一交易系統
  - 四種交易類型（租金、商人、商隊、互助）
  - 完整的交易驗證和配置載入機制

- **TenantManager.js** - 租客生命週期管理
  - 租客雇用/驅逐流程、滿意度系統、關係管理
  - 申請者生成、面試評估、衝突檢測

- **SkillManager.js** - 技能執行管理
  - 15個技能的執行邏輯（主動、被動、特殊技能）
  - 技能冷卻管理、效果處理

- **RentManager.js** - 租金計算與收取
  - 自動租金計算、收取流程
  - 租金優惠和懲罰機制

- **DayManager.js** - 每日事務處理
  - 每日推進邏輯、資源消耗計算
  - 租客日常活動處理

- **UniversalTrader.js** - 通用交易處理器
  - 統一交易介面、交易驗證
  - 支援多種交易類型和數據來源

### 🖥️ UI系統模組
處理使用者介面邏輯，與業務邏輯分離。

- **UICore.js** - 核心UI功能
  - 基礎介面控制和事件綁定
  - UI狀態管理

- **UIDisplay.js** - 畫面顯示邏輯
  - 狀態到UI的映射邏輯
  - 動態內容更新

- **UIModal.js** - 彈窗系統管理
  - 模態對話框管理
  - 事件選擇介面

## 🎯 技術架構特色

### 配置驅動設計
- **集中配置**：所有遊戲參數來自 JSON 檔案，零硬編碼
- **配置載入驗證**：配置載入失敗時明確錯誤提示，確保系統穩定性
- **熱更新支援**：修改配置檔案無需重新部署
- **參數驗證**：validators.js 提供完整驗證機制

### 事件驅動通信
- **模組解耦**：業務模組透過 EventBus 通信，避免直接依賴
- **統一介面**：標準化的事件發送/監聽機制
- **除錯友善**：完整的事件歷史和統計功能
- **效能最佳化**：事件過濾和節流控制

### 狀態集中管理
- **單一資料源**：GameState 作為唯一真實來源
- **狀態不變性**：深度複製保證狀態安全
- **歷史追蹤**：變更記錄用於除錯和回滾
- **路徑存取**：點記法路徑快速存取嵌套狀態

### 型別安全保證
- **JSDoc註解**：所有模組使用完整型別註解
- **TypeScript檢查**：透過 `// @ts-check` 啟用型別檢查
- **Type.js定義**：集中管理所有型別定義
- **編譯時驗證**：在開發階段捕捉型別錯誤

## 📊 架構品質指標

### 程式碼組織
- **模組獨立性**：每個模組平均 200-400 行，職責單一
- **依賴清晰度**：嚴格單向依賴鏈，零循環依賴
- **介面標準化**：所有模組遵循統一的介面規範
- **文檔完整性**：每個模組有詳細的JSDoc註解

### 系統穩定性
- **錯誤隔離**：單一模組錯誤不影響其他模組運作
- **明確錯誤處理**：配置載入失敗時提供清晰的錯誤訊息
- **資源保護**：防止記憶體洩漏和無限迴圈
- **瀏覽器相容**：原生ES6支援，無需額外工具

### 開發效率
- **並行開發**：不同模組可同時開發，依賴明確
- **即時測試**：dev-test.html 提供完整的測試環境
- **除錯便利**：內建統計、日誌、狀態監控功能
- **快速迭代**：配置驅動設計，參數調整無需改程式碼

## 🚀 部署與維護

### GitHub Pages 部署
- **零建置工具**：直接部署ES6模組，無需編譯步驟
- **即時更新**：推送到主分支自動部署
- **版本管理**：使用Git標籤管理版本
- **靜態資源**：所有資源透過相對路徑載入

### 維護友善設計
- **配置熱更新**：修改JSON檔案無需重新部署
- **模組獨立更新**：單一模組更新不影響其他功能
- **向下相容性**：新功能不破壞既有功能
- **效能監控**：內建系統狀態監控和統計
- **明確錯誤追蹤**：配置問題可快速定位和修復

## 🔧 開發指南

### 除錯與測試
```javascript
// 瀏覽器控制台除錯命令
gameApp.debug()                           // 顯示完整系統狀態
gameApp.dataManager.getSystemStatus()     // 檢查資料管理器
gameApp.gameState.getStateStats()         // 檢查遊戲狀態
gameApp.eventBus.getStats()              // 檢查事件系統

// URL參數啟用除錯模式
?debug=true                               // 啟用詳細日誌
```

### 配置修改
```javascript
// 修改 rules.json 即時生效
{
  "gameDefaults": {
    "initialResources": {
      "cash": 100  // 修改初始現金
    }
  }
}
```

### 系統狀態檢查
- **🟢 開發環境正常運行**：所有模組正常載入，完整功能可用
- **🔴 系統啟動失敗**：需要檢查瀏覽器ES6模組支援或配置檔案完整性

## 📚 相關文件

- [架構設計文件](docs/architecture.md) - 完整的技術架構說明
- [BaseManager 開發指南](docs/BaseManager-Guide.md) - 基礎管理器使用說明
- [系統流程圖集](docs/flow-diagrams.md) - 詳細的系統流程圖表

---

**專案狀態**：主要功能完成，採用現代化ES6模組架構，為持續發展和維護奠定堅實基礎。